services:

  web:
    build: .
    ports:
      - "8081:8080"
    volumes:
      - type: bind
        source: ./cl.json
        target: /var/cl.json
      - type: bind
        source: ./msg.json
        target: /var/msg.json
